// import { createContext, ReactElement, useState,} from "react";

// export const SizeWindowsContext = createContext(null);

// export const SizeWindowsProvider = ({children} :ReactElement) => {
//     const [sizeWindows, setSizeWindows] = useState(null);

//     return (
//         <SizeWindowsContext.Provider value={{ sizeWindows, setSizeWindows }}>
//           {children}
//         </SizeWindowsContext.Provider>
//       );
// }